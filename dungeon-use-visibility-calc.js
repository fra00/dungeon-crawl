/**
 * DO NOT EDIT THIS FILE DIRECTLY
 * ----------------------------------
 * This file was generated by ISL Compiler.
 * Source: dungeon-use-visibility-calc.isl.md
 * Edit the ISL file instead.
 */

import { useCallback } from 'react';
import { calculateVisibleCellsCore, hasLineOfSightCore } from './dungeon-visibility-calc-core.js';

export function useVisibilityCalc({ gameSession, visibilityMap }) {
    const calculateVisibleCells = useCallback((startX, startY) => {
        return calculateVisibleCellsCore(gameSession, visibilityMap, startX, startY);
    }, [gameSession, visibilityMap]);

    const hasLineOfSight = useCallback((startX, startY, targetX, targetY) => {
        return hasLineOfSightCore(gameSession, visibilityMap, startX, startY, targetX, targetY);
    }, [gameSession, visibilityMap]);

    return {
        calculateVisibleCells,
        hasLineOfSight
    };
}
